# 英文论文 → Notion 系统化整理工作流模板

> 用途：以后我只需要上传英文论文，并告诉你要整理到 Notion 的哪个页面。你需要自动完成论文信息填写、全文中文翻译、方法总结、图表插入提示、通俗注释、摘录和标签归档。

---

## 0. 我需要提供什么

我只需要提供两样东西：

1. **英文论文原文**
   - PDF 文件、arXiv 文件或其他论文全文。
2. **Notion 目标页面**
   - 例如：`请整理到 Notion 中的 TractFM 页面`
   - 或者直接给出 Notion 页面名称 / 页面链接。

---

## 1. 总体目标

你需要在我指定的 Notion 页面中完成以下工作：

1. 填写论文页面的元信息。
2. 将英文论文正文翻译为中文。
3. 保留并正确渲染行内公式和行间公式。
4. 在难理解的位置加入通俗注释。
5. 标记原文图片和表格应该插入的位置。
6. 在方法部分之前加入“方法总览”注释环境。
7. 完成论文摘录。
8. 生成并关联论文标签。
9. 如果当前 Notion 数据库中缺少期刊、会议或标签条目，需要自行新建并正确关联。
10. 参考文献部分不翻译、不复制、不放入 Notion 页面正文。

---

## 2. Notion 元信息填写规则

在目标 Notion 页面中填写或更新以下属性：

### 2.1 Title

填写论文完整英文标题。

示例：

```text
Tractogram foundation model
```

### 2.2 发表单位

填写作者主要单位，尽量凝练。

示例：

```text
浙江大学、天津大学、哈佛医学院、Brigham and Women’s Hospital
```

### 2.3 发表年份

根据论文年份填写。

示例：

```text
2026
```

### 2.4 期刊 / 会议

根据论文来源填写：

- 如果是期刊论文：填写期刊名称。
- 如果是会议论文：填写会议名称。
- 如果是 arXiv 预印本：填写 `arXiv` 或 `arXiv preprint`。
- 如果 Notion 的期刊/会议数据库中没有对应条目，需要新建条目并关联到当前论文页面。

### 2.5 GitHub 链接

如果论文中明确提供代码链接，则填写。

如果论文没有提供代码链接，则保持为空。

### 2.6 概括

概括需要重点围绕**方法部分**写，而不是简单复述摘要。

必须回答以下问题：

1. 这篇文章针对什么问题？
2. 现有方法有什么不足？
3. 作者提出了什么方法？
4. 方法的核心模块是什么？
5. 输入和输出是什么？
6. 得到了什么主要结论？
7. 该方法的意义是什么？

推荐格式：

```text
本文针对……问题，指出现有方法……。作者提出……方法，其核心思想是……。模型以……作为输入，通过……得到……输出。实验表明……。该工作说明……。
```

---

## 3. 正文翻译规则

### 3.1 翻译范围

需要翻译论文正文主体，包括：

- Abstract
- Introduction
- Related Work
- Results
- Methods
- Discussion
- Conclusion
- Data Availability
- Supplementary 方法性内容，如果正文中有

不需要翻译，也**不要放入 Notion 页面正文**：

- References / 参考文献
- Author contribution
- Acknowledgement
- Funding
- Conflict of interest
- 纯表格内容
- 纯图片内容

特别强调：**参考文献部分完全跳过**，不翻译、不复制、不总结，也不作为正文内容写入 Notion。

### 3.2 翻译风格

要求：

- 中文表达自然、准确、通俗。
- 保留必要的专业术语。
- 对初学者不友好的概念，需要在注释中解释。
- 不要逐字硬译，要保证中文可读性。
- 专业缩写第一次出现时给出英文全称和中文解释。

示例：

```text
dFC 是 dynamic functional connectivity，中文可译为“动态功能连接”。
```

### 3.3 公式渲染规则

所有公式都要使用 Notion 支持的公式环境。

#### 行内公式

行内公式使用 Notion 行内公式格式：

```text
$`X \in \mathbb{R}^{N\times 3\times P}`$
```

#### 行间公式

行间公式使用独立公式块：

```text
$$
L=\frac{1}{N}\sum_{i=1}^{N}\mathrm{CE}(\ell_i,y_i)
$$
```

### 3.4 图片和表格处理规则

图片和表格不用翻译，也不用插入。

但必须在正文对应位置加入提示，方便我后续截图插入。

图片提示模板：

```markdown
<callout icon="🖼️" color="blue_bg">
**图片放置建议：图 1**  
这里建议插入原文 Figure 1。该图展示……
</callout>
```

表格提示模板：

```markdown
<callout icon="🧾" color="blue_bg">
**表格放置建议：表 1**  
这里建议插入原文 Table 1。该表比较……
</callout>
```

### 3.5 图片/表格放置位置

图片和表格提示应放在最接近原文讨论的位置。

例如：

- 介绍整体模型框架后 → 放置 Figure 1。
- 讨论分割 / 分类性能后 → 放置 Table 1。
- 讨论嵌入可视化后 → 放置 Figure 2。
- 讨论注意力图后 → 放置 Figure 3。
- 讨论下游任务性能后 → 放置相关表格。
- 讨论消融实验后 → 放置 ablation 表格。
- 讨论数据集统计后 → 放置 dataset 表格。

### 3.6 参考文献处理规则

参考文献部分必须完全跳过：

- 不翻译 References。
- 不把 References 原文复制到 Notion。
- 不在 Notion 页面正文中生成参考文献列表。
- 如果正文翻译中出现引用编号，例如 `[1]`、`[23]`，可以保留编号以便对应原文，但不需要展开参考文献信息。

---

## 4. 标注 / 注释环境规则

对于重点和难理解内容，需要加入通俗注释。**所有注释必须放在 Notion 的标注环境中展示**，不要仅以普通段落形式散落在正文里。

这里的“标注环境”统一指 Notion callout。推荐使用以下格式：

```markdown
<callout icon="💡" color="gray_bg">
**注释：这里写注释标题**

这里用通俗语言解释概念、方法设计动机、公式含义或实验结论。
</callout>
```

### 4.0 标注环境硬性要求

- 注释、解释、类比、方法动机说明，必须写入 `<callout icon="💡" color="gray_bg">...</callout>` 中。
- 不要把注释写成普通正文段落。
- 如果注释内容较长，可以在 callout 内使用 `<details>` 折叠列表。
- 图表放置提示使用蓝色 callout，方法解释和概念解释使用灰色 callout。
- 标注环境中的语言要通俗，不要照搬英文原句。

### 4.1 哪些地方必须加注释

遇到以下内容时，优先加入注释：

1. 新手容易混淆的概念。
2. 方法设计动机。
3. 模型结构中的关键模块。
4. 损失函数含义。
5. 输入输出形状。
6. 实验设置为什么这样设计。
7. 消融实验说明什么。
8. 结果为什么有意义。
9. 与现有方法相比的关键差异。
10. 论文局限性和未来工作方向。

---

## 5. 方法总览注释环境

在正式翻译 Methods / Proposed Method / Methodology 之前，必须先加入一个凝练的方法总览。

该部分必须放在 Notion 的**标注环境（callout）**中，并尽量使用折叠列表。

模板如下：

```markdown
<callout icon="💡" color="gray_bg">
**方法总览：模型训练流程、输入输出、数据形状与损失函数**

<details>
<summary>一句话概括</summary>

用通俗语言概括该方法的核心思想。

</details>

<details>
<summary>1. 训练输入是什么？</summary>

说明模型输入数据是什么，以及形状是多少。

例如：

$$
X\in\mathbb{R}^{N\times 3\times P}
$$

</details>

<details>
<summary>2. 模型第一步做什么？</summary>

说明第一阶段模块、输入输出和形状变化。

</details>

<details>
<summary>3. 模型第二步做什么？</summary>

继续说明后续模块和形状变化。

</details>

<details>
<summary>4. 训练输出是什么？</summary>

说明最终输出是什么，例如分类 logits、重建结果、预测结果、分割图等。

</details>

<details>
<summary>5. 损失函数如何定义？</summary>

给出损失函数公式，并解释每一项含义。

</details>

<details>
<summary>6. 完整训练流程</summary>

用步骤列出完整训练流程。

</details>

<details>
<summary>7. 推理或迁移时如何使用？</summary>

说明训练后模型如何用于下游任务。

</details>

<details>
<summary>8. 输入输出总结</summary>

用简短项目符号总结训练阶段和推理阶段的输入输出。

</details>
</callout>
```

### 5.1 如果文章是深度学习模型论文

必须尽量说明：

- 输入是什么？
- 输入形状是什么？
- 每一步输出形状是什么？
- 编码器 / 解码器 / 分类头 / 预测头分别做什么？
- 训练标签是什么？
- 损失函数是什么？
- 推理阶段输出什么？
- 是否冻结模型？
- 下游任务怎么做？
- 数据增强怎么做？
- batch size、epoch、learning rate 等训练细节，如果论文提供，需要写明。

### 5.2 如果文章不是模型训练论文

则方法总览需要改为：

- 研究问题是什么？
- 数据来源是什么？
- 变量如何定义？
- 分析流程是什么？
- 统计方法是什么？
- 输出结果是什么？
- 结论如何得到？

---

## 6. 论文摘录规则

翻译和整理完成后，需要在当前论文页面关联的“论文摘录”数据库中创建摘录条目。

如果页面上有“添加摘录”按钮，但不能直接点击按钮，则需要：

1. 获取当前论文数据库 schema。
2. 找到“论文摘录”关系数据库。
3. 在摘录数据库中直接新建条目。
4. 将每条摘录关联回当前论文页面。

### 6.1 每条摘录包含字段

每条摘录至少包含：

- 摘录标题
- 摘录内容
- 我的笔记
- 类型
- 重要程度
- 关联论文

### 6.2 摘录内容类型

摘录应覆盖以下方面：

1. 论文核心工作。
2. 论文针对的问题。
3. 现有方法的挑战或不足。
4. 作者提出的关键方法。
5. 方法中的关键模块。
6. 重要实验结论。
7. 模型或方法的可迁移性。
8. 方法的局限性。
9. 未来工作方向。
10. 可复用的写作观点。

### 6.3 摘录数量

一般建议生成 10–20 条摘录。

如果论文内容较短，至少 8 条。

如果论文方法复杂，建议 15 条左右。

### 6.4 摘录示例

```text
摘录标题：全脑上下文对纤维束分区至关重要

摘录内容：模型通过全脑自注意力联合编码所有流线，使每条流线的表示不仅包含局部几何形态，还包含其在全脑白质结构中的上下文关系。

我的笔记：这一点适合写 related work 或 motivation：单条流线几何不足以区分相似或交叉纤维束，全脑上下文能显著提升解剖分区能力。

类型：观点、方法

重要程度：⭐⭐⭐ 非常重要
```

---

## 7. 标签归档规则

整理完成后，需要为论文生成标签并关联到当前页面。

如果当前标签库中没有合适标签，需要新建标签。

### 7.1 标签生成原则

标签应包含：

1. 研究领域标签
2. 数据模态标签
3. 方法类型标签
4. 任务类型标签
5. 模型结构标签
6. 是否可解释 / 可迁移 / 基础模型等特征标签

### 7.2 标签示例

对于 tractography foundation model 论文，可以生成：

```text
Diffusion MRI
Tractography
White Matter
Foundation Model
Representation Learning
Transformer
Self-Attention
Tract Parcellation
Phenotype Prediction
Transfer Learning
```

对于 rs-fMRI normative modeling 论文，可以生成：

```text
rs-fMRI
Normative Modeling
Explainable AI
Brain Disorder Identification
Graph Neural Network
Self-Supervised Learning
Dynamic Functional Connectivity
Anomaly Detection
```

---

## 8. Notion 操作规范

### 8.1 页面检索

当我说“整理到 Notion 中的某某页面”时：

1. 先搜索该页面。
2. 如果有多个相近页面，需要根据标题和上下文判断最可能的目标页面。
3. 如果仍然不确定，再向我确认。
4. 找到页面后，获取页面内容和数据库 schema。

### 8.2 数据库 schema

更新页面属性前，需要先 fetch 当前数据库 schema，确认字段名和字段类型。

尤其注意：

- 标题字段可能叫 `Name`
- 论文标题可能叫 `Title`
- 期刊可能是 relation
- 标签可能是 relation
- 摘录可能是 relation
- 发表年份可能是 text
- 整理人可能是 select
- 状态可能是 status

### 8.3 新建 relation 条目

如果期刊、会议或标签不存在：

1. 先 fetch 对应 relation 数据库。
2. 新建对应条目。
3. 再关联回当前论文页面。

### 8.4 按钮属性

如果页面上有按钮，例如：

- 添加摘录
- 生成标签

但工具不能直接点击按钮，则不要停止。

应根据按钮对应的关系数据库或页面结构，直接完成等价操作：

- 新建摘录条目
- 新建标签条目
- 关联到当前论文页面

---

## 9. 最终回复格式

完成后，向我简短说明你完成了哪些内容。

推荐格式：

```text
已完成。已在 Notion 的【页面名】页面中完成以下整理：

- 填写/更新论文元信息
- 翻译全文主体内容
- 正确处理行内公式与行间公式
- 添加通俗注释
- 标记图片和表格插入位置
- 在方法部分前添加方法总览
- 创建论文摘录
- 生成并关联标签

参考文献未翻译，也未放入 Notion 页面；图片和表格正文未翻译，方便你后续截图插入。
```

---

## 10. 一次性可复制提示词

下面这段可以作为以后直接复制给你的提示词。

```text
我上传了一篇英文论文，请你整理到 Notion 中的【页面名称或链接】页面。

你需要完成以下工作：

1. 阅读论文全文，并在 Notion 目标页面填写论文元信息，包括 Title、发表单位、发表年份、期刊/会议、GitHub 链接和概括。其中“概括”需要重点围绕方法部分，说明这篇文章针对什么问题、现有方法有什么不足、提出了什么方法、核心模块是什么、输入输出是什么、得到什么结论。如果期刊/会议数据库中没有对应条目，请自行新建并关联。

2. 在 Notion 页面正文中将论文主体翻译为中文。参考文献不用翻译，也不要放入 Notion 页面正文；图片和表格不用翻译。行内公式请使用 Notion 行内公式格式 $`...`$，行间公式请使用 $$...$$ 公式块。

3. 对新手难以理解的概念、方法动机、关键公式、实验设置和重要结论，请用 Notion 的标注环境（callout）进行通俗解释。注释内容必须放在标注环境中展示，不要写成普通正文段落。

4. 图片和表格不用插入，但你需要在正文中对应位置加入“图片放置建议”或“表格放置建议”，说明这里应该插入原文的 Figure / Table 几，以及该图表表达什么。

5. 在方法部分之前，新增一个“方法总览”注释环境。若论文涉及模型训练，请详细说明训练流程、输入输出、每一步数据形状、模型模块、损失函数、推理方式和下游任务使用方式，并尽量使用折叠列表组织。

6. 翻译完成后，请在当前页面关联的论文摘录数据库中创建重要观点摘录。摘录应包括论文核心工作、面临挑战、关键方法、实验结论、局限性和未来工作方向。每条摘录需要包含摘录标题、摘录内容、我的笔记、类型、重要程度，并关联回当前论文页面。

7. 请为该论文生成合适标签并关联到当前页面。如果标签库中没有合适标签，请自行新建标签。

如果页面上有“添加摘录”或“生成标签”等按钮，但你不能直接点击按钮，请根据当前数据库结构直接完成等价操作。
```
